[tools]
# Keep aligned with the `toolchain` directive in go.mod.
go = { version = "1.26.2" }
shellcheck = "0.11.0"

[tasks.fmt]
description = "Run gofmt"
run = "gofmt -s -w ."

[tasks.test]
description = "Run tests"
run = "go test ./..."

[tasks."test:ci"]
description = "Run tests with race detection"
run = "go test -race ./..."

[tasks.build]
description = "Build the plugin binary"
run = "go build -o entire-plugin-template ./cmd/entire-plugin-template"

[tasks.build-all]
description = "Cross-build common Entire CLI plugin targets"
run = '''
set -e
for target in darwin/amd64 darwin/arm64 linux/amd64 linux/arm64 windows/amd64; do
  GOOS="${target%/*}" GOARCH="${target#*/}" go build ./...
done
'''

[tasks.check]
description = "Run formatting, linting, tests, and builds"
depends = ["lint", "test:ci", "build-all"]
